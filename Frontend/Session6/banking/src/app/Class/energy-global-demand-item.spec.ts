import { EnergyGlobalDemandItem } from './energy-global-demand-item';

describe('EnergyGlobalDemandItem', () => {
  it('should create an instance', () => {
    expect(new EnergyGlobalDemandItem()).toBeTruthy();
  });
});
